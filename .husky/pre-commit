#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Executa lint-staged apenas nos arquivos modificados
npx lint-staged

# Roda testes rápidos (apenas arquivos modificados relacionados)
# Comentado por padrão - descomente quando tiver testes
# npm run test -- --findRelatedTests --passWithNoTests

