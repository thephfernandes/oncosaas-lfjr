services:
  postgres:
    image: postgres:15-alpine
    container_name: ONCONAV-postgres
    environment:
      POSTGRES_USER: ONCONAV
      POSTGRES_PASSWORD: ONCONAV_dev
      POSTGRES_DB: ONCONAV_development
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ONCONAV']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: ONCONAV-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: ONCONAV-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ONCONAV
      RABBITMQ_DEFAULT_PASS: ONCONAV_dev
    ports:
      - '5672:5672'
      - '15672:15672'
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ['CMD', 'rabbitmq-diagnostics', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:
